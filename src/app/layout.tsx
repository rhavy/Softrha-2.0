import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Toaster } from "@/components/ui/toaster";
import "./globals.css";
import PublicLayout from "@/components/layout/public-layout";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SoftRha | Desenvolvimento de Software de Alta Performance",
  description: "Transformando ideias em soluções digitais com Next.js 14, TypeScript e Tailwind CSS. Especialistas em desenvolvimento web, mobile e software sob medida.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <PublicLayout>{children}</PublicLayout>
        <Toaster />
      </body>
    </html>
  );
}
